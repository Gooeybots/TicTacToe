#ifndef WINDOW_HPP
#define WINDOW_HPP
/* Opens a window and sets the resize callback to keep viewport square
 * returns false if anything goes wrong */
bool SetupWindow(const char * title, const int width, const int height);

#endif // WINDOW_HPP
